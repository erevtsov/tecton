# synqvest

### Notes on env setup:

#### Install uv
curl -LsSf https://astral.sh/uv/install.sh | sh

#### sync dependencies
uv sync

#### .bashrc
source ~/.local/bin/env
#source .venv/bin/activate

### TODOs
- lookback capability